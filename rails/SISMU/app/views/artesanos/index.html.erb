<main class="container sombra">
    <section class="productos">
        <%# <div class="box">
            <%= form_tag artesanos_path, :method => 'get' do %
              <p>
                <%= text_field_tag :search, params[:search] %
                <%= submit_tag "Buscar", :id => nil  %
              </p>
            <% end %
        </div> %>

        <div class="buscador_artesanos">
          <div class="prueba_artesanos">
            <%= form_tag artesanos_path, :method => 'get' do %>
              <%= text_field_tag :search, params[:search], placeholder: "Buscar"%>
              <div class="btn_buscar_artesanos">
                <button class="btn_borrar">
                  <%= image_tag "iconos/buscar_blanco", :referencia_pro => nil, class: "logoBuscar_artesanos" %>
                </button>
              </div>
            <% end %>
          </div>
        </div>

        <h1 class="artesanos__titulo">Artesanos</h1>
        <%= link_to "artesanos/new" do %>
          <div class="artesanos__nuevo">
            <p>Nuevo</p>
          </div>
        <% end %>
    </section>

    <section class="artesanosTable">
        <table>
            <thead>
                <tr>
                <th><%= sort_link(@q, :id,"Cedula", default_order: :desc, class:"white") %></th>
                <th><%= sort_link(@q, :nombre,"Primer Nombre", default_order: :desc, class:"white") %></th>
                <th><%= sort_link(@q, :apellido,"Primer Apelldio ", default_order: :desc, class:"white") %></th>
                <th colspan="3">Acción</th>
                </tr>
            </thead>
            <tbody>
                <%= render @artesanos%>
            </tbody>
        </table>
    </section>

  <div class="digg_pagination">
    <%= will_paginate(@artesanos,:previous_label => "&laquo;", :next_label => "&raquo;",:class=>"small-font",:outer_window => 1,:inner_window => 0) %>
  </div> 

</main>